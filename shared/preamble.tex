% Shared LaTeX preamble — TODO: extract common packages/macros from base/*/main.tex
% into here so resume + cover-letter share styling.
%
% Until then, each resume and cover-letter is self-contained.
%
% When ready, in resume.tex / cover-letter.tex use:
%   \input{../../shared/preamble.tex}
